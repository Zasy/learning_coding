//
// Created by zhanGGe on 2018/9/4.
// 不修改数组找出重复的数字
// 第一种浪费空间的方法 利用一个计数的数组去对出现的数字进行计数
// 第二种

bool getDuplicate(vector<int> data, int len) {

}


int main() {
    int myints[] = {1,2,3,3};
    vector<int> test (myints, myints + sizeof(myints) / sizeof(int) );

    getDuplicate(test);

    return 0;
}

